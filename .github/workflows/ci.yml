name: CI-Workflow

on: 
  push: 
    branches: [develop]
  pull_request:
    branches: [develop]

jobs: 
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        test-type: [unit, integration]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      # Garante que o Jest tenha permissão de execução no ambiente Linux do GitHub Actions
      - name: Fix Jest Permissions
        run: chmod +x ./node_modules/.bin/jest

      - name: Run ${{ matrix.test-type }} tests
        run: |
          if [ "${{ matrix.test-type }}" = "unit" ]; then
            echo "Running unit tests with mock DB..."
            npx jest --testPathPattern="Testes/Unitários"
          else
            echo "Running integration tests with mock DB..."
            npx jest --testPathPattern="Testes/Integração"
          fi
        env:
          USE_MOCK_POOL: true
